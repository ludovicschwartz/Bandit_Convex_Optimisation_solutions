%! TEX root = ./main.tex
\begin{exercise}[]{}
\end{exercise}
Prove the following statements:

(a) Given convex bodies $K\subset J$, polarity reverses inclusion:$J \subset K$.

(b) The polar body of a non-empty ball $K = \mathbb{B}_r^d$ is $K = \BB_{1/r}^d.$

(c) For symmetric convex body $K$, $||\cdot||_K := \pi_K(\cdot)$ is a norm.

(d) For symmetric convex body $K$, the dual of $||\cdot||_K$ is $||\cdot||_{K}$:
\[ ||u||_{K} =\max\{\siprod{u}{x}: ||x||_{K}\leq 1, x \in \real^d\}.\]
\begin{solution}[TODO]
\end{solution}
